node_modules/
dist/
dist-cjs/
coverage/
*.tsbuildinfo

# Environment files - never commit real credentials
.env
.env.local
!.env.example

*.log
.DS_Store

# Docker volumes are managed by Docker, not git
docker/seed/*.archive

# Agent scratch space: handoff notes and throwaway test scripts. Deliberately not committed - these
# are local working notes, and may reference private deployment details.
agents/

# Local agent tool permissions - machine-specific, not shared
.claude/
